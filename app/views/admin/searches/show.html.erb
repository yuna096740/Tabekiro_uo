<div class="container">
  <div class="row my-5 mx-auto">
    <div class="col-md-10 offset-1">
      <div class="d-flex my-3">
        <h4>通報合計：<%= @reports.count %>回</h4>
        <h4>ユーザー名：
          <%= link_to admin_member_path(@member) do %>
            <%= @member.nickname %>
          <% end %>
      </div>
      </h4>
        <%= render 'admin/reports/index', reports: @reports %>
    </div>
  </div>
</div>