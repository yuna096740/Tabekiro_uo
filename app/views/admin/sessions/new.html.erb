<div class="container">
  <div class="row mx-auto mb-5">
    <div class="col-md-6 col-sm-8 col-xs-12 offset-md-3 mb-5" data-aos="zoom-in" data-aos-duration="1000">
      <%= form_with model: @admin , url: session_path(resource_name) do |f| %>
        <div class="text-center">
          <div class="session_logo">
            <%= image_tag 'Tabekiro-uo-logo.svg' %>
            <h4>管理者ログイン</h4>
          </div>
        </div>

        <div class="card-body">
          <span class="card-text">
            <%= f.label :email, 'メールアドレス' %><br />
            <%= f.email_field :email, autofocus: true, autocomplete: "email", class:"form-control" %>
          </span>

          <span class="card-text">
            <%= f.label :password, 'パスワード' %><br />
            <%= f.password_field :password, autocomplete: "current-password", class:"form-control" %>
          </span>

          <div class="card-text">
            <%= f.submit "ログイン", class:"btn btn-outline-secondary form-control mt-2" %><br>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>