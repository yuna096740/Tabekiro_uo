<div class="container">
  <div class="row mb-5">
    <% @posts.each do |post| %>
      <div class="col-4 mb-5">
        <%= link_to post_path(post) do %>
          <%= image_tag post.post_image, size: "350x300" %>
          <div class="text-dark"><%= post.title %></div>
        <% end %>
      </div>
    <% end %>
  </div>
</div>