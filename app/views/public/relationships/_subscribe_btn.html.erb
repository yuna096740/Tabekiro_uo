<% if current_member.subscribing?(member) %>
  <%= link_to '- UNSUBSCRIBE', member_relationships_path(member), method: :delete, remote: true, class:"btn btn-info form-control col-md-9" %><br/>
<% else %>
  <%= link_to '＋ SUBSCRIBE', member_relationships_path(member), method: :post, remote: true, class:"btn btn-danger form-control col-md-9" %><br/>
<% end %>
